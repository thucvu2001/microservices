server:
  port: 8081

grpc:
  server:
    port: 9091

spring:
  application:
    name: authentication-service
  profiles:
    active: @spring.profiles.active@

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

logging:
  level:
    root: INFO
    web: INFO